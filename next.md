下一步的挑戰：登入 (Login) 與 核發通行證 (JWT)
有了「註冊」，下一個必然要做的就是「登入」。登入背後牽涉到一個現代網站最重要的觀念：狀態管理與權限。

當使用者順利輸入帳號密碼登入後，我們不可能每一次他要買東西，都要他重新輸入密碼。業界大型專案解決這個問題的標準答案就是：JSON Web Token (JWT)。

登入 API 的邏輯會是這樣的：

Controller：接收前端傳來的 { "account", "password" }。
Service：去資料庫尋找這個帳號。
Service：使用 bcrypt.compare 來比對前端給的明文密碼，與資料庫的亂碼（雜湊值）是否吻合。
Service (重頭戲)：如果吻合，後端會製作一張具有時效性（例如 7 天）、且無法被偽造的「數位通行證 (JWT)」。
Controller：把這張 JWT 派發給前端。之後前端發送任何請求都要帶著這張 JWT，我們就不用再去查密碼了。
您準備好迎接這個挑戰了嗎？準備好的話請告訴我，我們就來裝 JWT 套件，繼續「一步一步」把它寫出來！
